name: dartwitter
description: A Twitter API client library for Dart developers.
version: 0.0.5

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  http: ^0.12.0+4
  oauth1_client:
    git: https://github.com/MertcanDinler/oauth1_client
  

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
  build_runner: ^1.8.1
